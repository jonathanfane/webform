kind: Service
apiVersion: v1
metadata:
  name: webform
spec:
  selector:
    app: webform
    tier: frontend
    track: bluegreen
  ports:
    - protocol: TCP
      port: 80
      targetPort: http
      nodePort: 31000
  type: NodePort
